{
  "name": "inner-voice-mvp",
  "version": "1.0.0",
  "description": "Voice Assistant Demo",
  "main": "server.js",
  "homepage": "https://krrrDiana.github.io/IV",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "node --require dotenv/config server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "voice",
    "assistant"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.30.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}